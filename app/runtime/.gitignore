# =====================================================================
#  app/runtime/ — 运行时依赖的静态数据 & 临时产物目录（项目约定）
#
#  规则（见 .trae/rules/global-style.md §12）：
#   ✅ 应该加入仓库（保留白名单）：
#     - static/          运行时依赖的静态数据（币种币种白名单、Symbol 配置 JSON、
#                           交易对映射、节假日日历、种子 OHLCV 样本等）
#     - trader/data/     回测使用的种子 OHLCV CSV（体积可接受，便于团队共享）
#     - 任何 业务运行 必须的输入数据（缺了代码会报错）
#
#   ❌ 忽略（不进仓库）：
#     - *.pid            服务进程 PID 文件（框架运行时生成）
#     - cache/           Config/路由/类映射缓存（启动时自动重建）
#     - trader/output/   回测导出（JSON/CSV，每次运行重新生成）
#     - *.log            日志（日志统一走项目根 logs/）
#     - *.tmp / *.lock   临时文件 / 锁文件
# =====================================================================

# ---- 默认忽略全部动态文件 ----
*.pid
*.lock
*.tmp
cache/
trader/output/

# ---- 保留静态数据子目录（放 .gitkeep 保证 git 追踪空目录）----
#  .gitignore 负向规则示例（如未来需要追踪 trader/data 下 .csv 则补充）：
#  !trader/data/*.csv
#  !static/*.json
